let nota = 6;

let status = nota > 7?"Aprovado":"Reprovado"

console.log(status);

let dia_semana = 0;

let status_loja = (dia_semana === 0) || (dia_semana === 6) ? "Funcionamos apenas de segunda a sexta":"Loja aberta";

console.log(status_loja);

